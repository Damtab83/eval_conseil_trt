{% extends 'base.html.twig' %}

{% block title %}Edit Notice{% endblock %}

{% block body %}
    <h1 class="text-center mt-4">Edit Notice</h1>

    <div class="container">
        {{ include('notice/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="d-flex mb-10">
            <div class="me-4">
                <a href="{{ path('app_notice_index') }}" class=" btn btn-primary mt-4">back to list</a>
            </div>
            <div class="mx-4">
                {{ include('notice/_delete_form.html.twig') }}
            </div>
        </div>

    </div>
{% endblock %}
